name: Publish critical signed APT repairs
# Compatibility dispatch for existing callers. All publishers now use the same
# complete candidate selection, gates, signing chain and concurrency group.
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  complete-publication:
    permissions:
      contents: write
    uses: ./.github/workflows/publish-apt-index.yml
    secrets: inherit
